# Environment & secrets
.env
.env.*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Node
node_modules/
dist/
dist.new/

# OS
.DS_Store

# 本地产物,不进版本库
.trash/
backups/

# 本地运行时/用户状态（持仓、自选、重点关注、交易控制、备注）
# 这些文件不是源码；部分旧基线仍在跟踪，后续需在本机备份后再安全取消跟踪。
/portfolio.json
/watchlist.json
/focus.json
/stock_notes.json
/auto_trade_*.json
/backend.log

# 构建/缓存垃圾（迁移收尾清理）
backend/backend/
backend/api/backend/
frontend/vite.config.js.timestamp-*.mjs
frontend/dist_locked/
frontend/dist_old_*/
frontend/.shotcache/
*.bak.*
backend/reports/daily/
backend/api/data/
backend/api/hermes_native/reports/
.workbuddy/

# 外部子系统（用户要求全部上传，故取消忽略；仅保留依赖/密钥/产物忽略）
# 运行时缓存
backend/api/vibe/.cache/

# 外部工具(free-llm-gateway 等)在仓库根创建的 .hermes/ 日志目录，非 AIROBOT 代码
/.hermes/
