# Ai-Robot 项目规则

## 项目定位

Ai-Robot 是一个面向 A 股、港股和美股的市场数据、因子研究、选股和交易研究系统。

## 目录边界

- `backend/`：FastAPI 接口、采集服务、数据库模型、因子和量化逻辑。
- `frontend/`：React 前端和页面路由。
- `research/`：因子研究、验证和实验材料。
- `docs/`：设计方案、审计报告和迁移记录。
- `tests/`、`backend/tests/`、`v2_app/tests/`：测试代码。
- `stock-tools/`：独立工具或实验模块，修改前先确认是否仍在生产链路中。

## 修改规则

1. 修改前先用精准搜索定位路由、接口、模型和调用方，不扫描无关目录。
2. 旧系统功能默认保留；删除、停用或迁移必须有明确依据并记录影响。
3. 不改变已有 API 响应字段和数据库字段语义，确需变化时提供兼容层或迁移脚本。
4. 因子必须说明数据来源、计算周期、方向、缺失处理和是否允许进入生产评分。
5. 严禁未来函数、使用未完成交易日数据、静默伪造行情或用默认分数掩盖缺失数据。
6. 采集、数据库和评分链路的改动必须配套测试；前端改动必须验证构建和关键路由。
7. 不修改 `.git/`、缓存、构建产物、备份和数据库文件来解决代码问题。
8. 修改完成后报告：改动文件、接口影响、测试命令和已知限制。

## 常用验证

```bash
python -m compileall backend
pytest -q backend/tests tests v2_app/tests
cd frontend && npm run build
```

按任务范围选择验证命令，不要为了收集上下文读取整个仓库。

## 数据与安全

- 数据流固定为“外部数据源 → 自动采集任务 → 数据库 → 查询接口/策略/页面”，不得改回查询时临时外采。
- GET、SSE 和其他读取接口只能读取数据库（或由数据库结果生成的短时缓存），不得因缺数触发联网、补采、回填或写库。
- 行情、财务、新闻、因子和策略快照在被业务使用前必须先落库；实时需求也应由独立采集器先写入快照/明细表。
- 数据库缺失、过期或样本不足时必须返回明确的 `MISSING`、`STALE` 或 `INSUFFICIENT` 状态；严禁用外部即时结果、合成数据或不同截止日的数据静默兜底。
- 不把密钥、Cookie、个人数据或本地数据库内容写入文档索引。
- Repomix 只索引源码、配置样例、文档和测试；排除数据库、构建产物、缓存、备份和日志。
